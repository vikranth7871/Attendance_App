# Project Rules

## Git Workflows
- **NEVER execute `git push`** or push commits to any remote repository unless the user explicitly commands you to push.
- Keep all changes, edits, and commits local until explicitly asked by the user to push.
